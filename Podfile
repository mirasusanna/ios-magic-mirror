# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Magic Mirror' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Magic Mirror
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Apollo', '~> 0.9'

  target 'Magic MirrorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Magic MirrorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
